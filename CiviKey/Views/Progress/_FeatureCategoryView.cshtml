@model ICollection<CiviKey.ViewModel.FeatureViewModel>
@{
}
@foreach( var item in Model )
{
    <div class="progress-list-elem" data-route="@Url.Action( "Index", "Feature", new { id = item.Id } )">
        @Html.Partial( "_FeatureView", item )
    </div>
}
<div style="clear: both;">
</div>
